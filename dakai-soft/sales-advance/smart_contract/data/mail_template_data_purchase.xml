<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="template_contract_email_purchase" model="mail.template">
            <field name="name">Contract Email Template</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="subject">Contract pentru achizitia #${object.name}</field>
            <field name="email_from">${(object.user_id.email or 'noreply@example.com')}</field>
            <field name="email_to">${object.partner_id.id}</field>
            <field name="body_html">
                <![CDATA[
                <p>Bună ziua,</p>
                <p>Vă trimitem atașat contractul pentru achizitia <b>${object.name}</b>.</p>
                <p>Cu stimă,<br/>Echipa noastră</p>
            ]]>
            </field>
        </record>
    </data>
</odoo>